document.addEventListener('DOMContentLoaded', () => {

    /* ==========================================
       1. SCROLL REVEAL (Intersection Observer)
       ========================================== */
    // A API Intersection Observer vigia os elementos e deteta quando eles entram no ecrã
    const observerOptions = {
        root: null,
        rootMargin: '0px',
        threshold: 0.15 // O efeito dispara quando 15% do elemento está visível
    };

    const observer = new IntersectionObserver((entries, observer) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                // Adiciona a nossa classe CSS que tem os Keyframes
                entry.target.classList.add('animar-entrada');
                // Performance: Para de vigiar depois da animação acontecer a 1ª vez
                observer.unobserve(entry.target);
            }
        });
    }, observerOptions);

    // Seleciona todos os elementos no HTML que têm a classe 'revelar-scroll'
    document.querySelectorAll('.revelar-scroll').forEach((el) => {
        // Esconde o elemento inicialmente via JS (para quem não tem JS, o site continua a funcionar perfeitamente - Ponto de Acessibilidade!)
        el.style.opacity = '0';
        observer.observe(el);
    });

    /* ==========================================
       2. BOTÕES MAGNÉTICOS (Micro-interação)
       ========================================== */
    const magneticButtons = document.querySelectorAll('.btn-branco, .btn-contorno');

    magneticButtons.forEach(btn => {
        btn.addEventListener('mousemove', (e) => {
            const position = btn.getBoundingClientRect();
            // Calcula a distância do rato ao centro do botão
            const x = e.clientX - position.left - position.width / 2;
            const y = e.clientY - position.top - position.height / 2;

            // Move o botão 30% da distância horizontal e 50% da vertical
            btn.style.transform = `translate(${x * 0.3}px, ${y * 0.5}px)`;
        });

        btn.addEventListener('mouseout', () => {
            // Quando o rato sai, o botão dá um "salto" de volta ao centro (efeito mola)
            btn.style.transform = 'translate(0px, 0px)';
            btn.style.transition = 'transform 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275)';
        });

        btn.addEventListener('mouseenter', () => {
            // Remove a transição enquanto está a seguir o rato para não ter lag
            btn.style.transition = 'none';
        });
    });

    /* ==========================================
       3. ANO AUTOMÁTICO NO FOOTER
       ========================================== */
    const currentYearElement = document.getElementById('ano-atual');
    if (currentYearElement) {
        currentYearElement.textContent = new Date().getFullYear();
    }

});